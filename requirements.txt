# Runtime dependencies. Pin exact versions for reproducible deploys.

Django==5.1
python-dotenv==1.0.0      # Loads .env into the environment
Pillow>=10.2.0            # Image decoding and upload validation
whitenoise==6.6.0         # Serves hashed static files without a separate server

# Optional, for a production WSGI server on Unix/Linux (not Windows):
# gunicorn==21.2.0
#
# Optional, to run against PostgreSQL instead of SQLite:
# psycopg[binary]==3.2.1
